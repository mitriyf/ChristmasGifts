package ru.mitriyf.christmasgifts.utils.actions;

public enum ActionType {
    PLAYER, TELEPORT, MESSAGE, ACTIONBAR, CONNECT, TITLE, SOUND, EXPLOSION, EFFECT, CONSOLE, BROADCAST, BOSSBAR, PARTICLE, LOG, DELAY
}